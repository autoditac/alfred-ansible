# This is an OpenOCD script that connects Raspberry Pi to the STM32 and flashes the firmware.

init
targets
reset halt

echo "----Flashing----"

program /home/pi/sunray_install/firmware/rm18.ino.bin 0x08000000 verify reset exit
