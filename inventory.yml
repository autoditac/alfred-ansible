---
all:
  children:
    mowers:
      hosts:
        mower-a:
          ansible_host: batman.local
          ansible_user: pi
          alfred_board: rpi4       # rpi4 | bananapi
          alfred_mcu: main         # main | perimeter
        # mower-b:
        #   ansible_host: mower-b.local
        #   ansible_user: pi
        #   alfred_board: rpi4
        #   alfred_mcu: main
